# Agent instructions

Follow the repository workflow in `../CLAUDE.md`.

## Standing command authorization

The user explicitly authorizes `git`, `gh`, and `lax` commands needed to complete requested work. **Do not ask for permission or confirmation to use these commands again**, in this session or future sessions.

This includes fetching, staging, committing, merging, pushing, worktree creation and cleanup, archive synchronization, builds, and requested retractions and submissions. Treat this as standing authorization that persists across turns and task boundaries. Reuse existing command approvals, preserve unrelated work, and stay within the user's requested scope.
